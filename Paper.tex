\documentclass[final,3p,times]{elsarticle}

%% For two-column layout, use:
% \documentclass[final,3p,times,twocolumn]{elsarticle}

\usepackage[utf8]{inputenc} % allow utf-8 input
\usepackage[T1]{fontenc}    % use 8-bit T1 fonts
\usepackage{url}            % simple URL typesetting
\usepackage{booktabs}       % professional-quality tables
\usepackage{amsmath}        % math
\usepackage{amssymb}        % math symbols
\usepackage{nicefrac}       % compact symbols for 1/2, etc.
\usepackage{microtype}      % microtypography
\usepackage[linesnumbered,ruled,vlined]{algorithm2e}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{caption}
\usepackage{float}
% \usepackage{hyperref}

\graphicspath{{media/}{images/}}     % organize your images and other figures under media/ folder

\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.8}
\renewcommand{\textfraction}{0.07}
\renewcommand{\floatpagefraction}{0.8}
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{5}

\lstset{
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color[rgb]{0.0, 0.5, 0.0}\itshape,
    stringstyle=\color{orange},
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=10pt,
    backgroundcolor=\color{white},
    showstringspaces=false,
    breaklines=true,
    captionpos=b,
    frame=single,
    rulecolor=\color{black},
    tabsize=2,
}

% Algorithm2e commands
\SetKwBlock{Begin}{begin}{end}
\SetKwFor{ForEach}{for each}{do}{endfor}
\SetKwIF{If}{ElseIf}{Else}{if}{then}{else if}{else}{endif}
\SetKwFor{While}{while}{do}{endwhile}
\SetKwFor{For}{for}{do}{endfor}
\SetKw{KwTo}{to}
\SetKw{Continue}{continue}
\SetKw{Break}{break}
\SetKw{Return}{return}
\SetKw{Downto}{downto}

\journal{Computer Physics Communications}
\renewcommand{\bibfont}{\normalfont\normalsize}

%% ============================================================
%% Numeric-claim macros — every quantitative claim in the body
%% is bound to one of these. Headline HGCAL values use N=5M, k=40
%% unless otherwise noted.
%% ============================================================

% Per-dimension speedups vs FAISS-GPU IndexFlatL2
\newcommand{\spdHGCALdtwo}{3.42\ensuremath{\times}}
\newcommand{\spdHGCALdthree}{2.96\ensuremath{\times}}
\newcommand{\spdHGCALdfour}{2.30\ensuremath{\times}}
\newcommand{\spdHGCALdfive}{3.09\ensuremath{\times}}
\newcommand{\spdHGCALdsix}{10.51\ensuremath{\times}}
\newcommand{\spdHGCALdseven}{35.23\ensuremath{\times}}
\newcommand{\spdHGCALdeight}{42.46\ensuremath{\times}}
\newcommand{\spdHGCALdnine}{32.68\ensuremath{\times}}
\newcommand{\spdHGCALdten}{29.83\ensuremath{\times}}

% vs cuVS brute force (strongest exact GPU baseline)
\newcommand{\spdHGCALcuvsdtwo}{1.65\ensuremath{\times}}
\newcommand{\spdHGCALcuvsdfive}{1.48\ensuremath{\times}}
\newcommand{\spdHGCALcuvsdsix}{4.92\ensuremath{\times}}
\newcommand{\spdHGCALcuvsdeight}{20.15\ensuremath{\times}}
\newcommand{\spdHGCALcuvsdten}{14.31\ensuremath{\times}}

% vs CAGRA-NN-Descent (approximate GPU baseline, after IVF-PQ fix)
\newcommand{\spdHGCALcagradtwo}{1.45\ensuremath{\times}}
\newcommand{\spdHGCALcagradsix}{4.40\ensuremath{\times}}
\newcommand{\spdHGCALcagradeight}{17.36\ensuremath{\times}}
\newcommand{\spdHGCALcagradten}{13.67\ensuremath{\times}}

% Geomeans across d=2-10 at N=5M, k=40
\newcommand{\geoFaiss}{10.1\ensuremath{\times}}
\newcommand{\geoCuvs}{4.8\ensuremath{\times}}
\newcommand{\geoCagra}{4.3\ensuremath{\times}}

% Reference-cell wall-clocks at d=8, N=5M, k=40
\newcommand{\fgcPCADeight}{7.2\,s}
\newcommand{\faissDeight}{306\,s}
\newcommand{\cuvsBFDeight}{146\,s}
\newcommand{\cagraNNDDeight}{127\,s}

% Internal ablation reference cells (d=8, N=5M, k=40)
% Binary A median, d=8 N=5M k=40, axis best within released kernel
% surface (mbd in {2..5}), at mbd=2 -- see Section sec:mbd-ablation.
\newcommand{\fgcVanillaBestDeight}{199.37\,s}
\newcommand{\fgcVanillaBestPCAfactor}{26.6\ensuremath{\times}}
% Ablation reference cells.
\newcommand{\onePassMBDoptPCA}{7.48\,s}
\newcommand{\onePassMBDoptAxis}{199.37\,s}

% Synthetic GPU speedup macros. Binary A, N=1M k=40, Gaussian N(0,I_d),
% median of 3 reps. FastGraph vs cuVS BF (the fastest exact GPU baseline
% on isotropic data). FastGraph is slower at d>=7; we drop those macros and
% report the synthetic-data contrast in a small table inside sec:synthetic.
\newcommand{\spdSynthDthree}{55\ensuremath{\times}}
\newcommand{\spdSynthDfive}{4.8\ensuremath{\times}}

\newcommand{\hgcalRecHits}{1.16\,billion}

% max\_bin\_dims (k) optima from ablation
\newcommand{\mbdOptPCA}{3}
\newcommand{\mbdOptAxis}{2}
\newcommand{\mbdPCAdefault}{3}

% Backwards-compatible alias for the CAGRA slowdown at d=8 cited in Limitations
\newcommand{\cagraSlowdownDeight}{\spdHGCALcagradeight}

\begin{document}

\begin{frontmatter}

\title{FastGraph: PCA-Subspace Binned $k$-Nearest-Neighbor Graph Construction for GPU Geometric Deep Learning}

%% Authors
\author[cmu]{Aarush Agarwal}
\ead{aarusha@andrew.cmu.edu}

\author[cmu]{Raymond He}
\ead{rhe2@andrew.cmu.edu}

\author[kit]{Jan Kieseler}
\ead{jan.kieseler@cern.ch}

\author[cmu]{Matteo Cremonesi\corref{cor1}}
\ead{mcremone@andrew.cmu.edu}

\author[uzh]{Shah Rukh Qasim}
\ead{shah.rukh.qasim@cern.ch}

\cortext[cor1]{Corresponding author}

%% Affiliations
\affiliation[cmu]{organization={Carnegie Mellon University},
            city={Pittsburgh},
            state={PA},
            country={USA}}

\affiliation[kit]{organization={Karlsruhe Institute of Technology},
            city={Karlsruhe},
            country={Germany}}

\affiliation[uzh]{organization={University of Zurich},
            city={Zürich},
            country={Switzerland}}

\begin{abstract}
Dynamic graph neural networks repeatedly construct $k$-nearest-neighbor
graphs in learned latent spaces, making exact GPU-resident graph
construction a central bottleneck for scientific point-cloud workloads.
We introduce \textbf{FastGraph}, a PyTorch-integrated primitive for exact
$k$NN graph construction in low-to-moderate-dimensional learned spaces.
The core contribution is a PCA-subspace cell-list formulation: FastGraph
fits a compact binning coordinate system to the input coordinate tensor
and uses that subspace for
spatial pruning, and evaluates all candidate distances in the original
feature space. This removes the dimensional cap of axis-aligned cell
lists while preserving exact neighbor sets, extending uniform-grid
pruning to the $d{=}4$--$10$ latent spaces used in modern HGCAL
reconstruction models. The implementation is GPU-resident end to end and
ships with an eager-mode PCA API, an opt-in differentiable
\texttt{GravNetOp} integration, and an object-condensation helper kernel for particle
reconstruction workflows. On a 5\,M-point CMS HGCAL recHit workload,
FastGraph builds the exact graph in \fgcPCADeight\ at $d{=}8$,
$k{=}40$, achieving speedups of up to $40\times$ over the exact GPU
baselines and over $17\times$ over approximate CAGRA while retaining
exact neighbor sets under the paper's distance-based convention.
\end{abstract}

\begin{keyword}
Graph Neural Networks \sep $k$-Nearest Neighbors \sep GPU Acceleration \sep CUDA \sep PCA \sep Object Condensation \sep Particle Physics \sep Differentiable Computing
\end{keyword}

\end{frontmatter}

\section{Introduction}

Graph neural networks (GNNs) have become a standard substrate for
reasoning over irregular relational data, from web-scale recommender
systems~\cite{ying2018pinsage} to particle reconstruction in
high-granularity calorimeters~\cite{Qasim2019GravNet,Kieseler2020ObjectCondensation}.
A defining architectural choice in many modern GNNs is that the graph
itself is built at training time, from a learned latent embedding, by
a $k$-nearest-neighbor query.
The dynamic kNN-graph layer is therefore on the critical path of
every forward and backward pass; its construction cost frequently
dominates the model's wall-clock budget once the latent dimensionality
is anything other than two or three. Importantly, the latent
dimensionality in the target application is small but not trivially so: the original
GravNet layer for HGCAL reconstruction~\cite{Qasim2019GravNet} uses
$d{=}4$ for the learned space in which the kNN is taken, and recent
multi-particle and object-condensation variants used in CMS HGCAL
reconstruction~\cite{Kieseler2020ObjectCondensation,qasim2021multiparticle,bhattacharya2022gnn}
sit in the $d{=}4$--$10$ range. EdgeConv~\cite{wang2019dynamic} and
ParticleNet~\cite{qu2020particlenet} also rebuild feature-space graphs,
but later layers may operate at substantially higher dimensionality and
are outside the primary regime evaluated here. This
\emph{small-but-not-tiny} regime is precisely where neither generic
high-dimensional ANN libraries nor brute-force GPU $k$NN are an ideal
fit: ANN libraries introduce unfavorable overheads, and brute force
scales as $N^2 d$ regardless of how much spatial structure the latent
embedding contains.

\paragraph*{Why exact $k$NN matters in this regime.}
For scientific workloads where the underlying topology must be
faithfully recovered, the dynamic-graph layer must also be exact, not
just high-recall. In CMS HGCAL shower reconstruction the neighbors
that just-barely-fall inside or outside the $k$th-nearest cut are
the structurally informative edges---points that separate one shower
from a neighboring shower. An approximate backend at $\text{recall}{=}0.9$
does not lose 10\% of arbitrary edges; it preferentially loses the
boundary edges that the GNN most needs, and the loss varies
non-deterministically across reruns. This introduces a
recall-dependent systematic into training gradients (the dynamic
graph fluctuates from epoch to epoch in a way that's a function of
the index implementation, not the data) and into inference
(downstream physics observables---cluster energy, shower
identification---become a function of which neighbors the
approximate index happened to return). This is why CMS HGCAL
reconstruction pipelines use exact $k$NN in the dynamic-graph layer
even when faster approximate alternatives are available.

The classical low-dimensional accelerator for this regime is the
\emph{cell list}~\cite{beyer1999nearest}: place points in a uniform
grid, expand neighboring bins around each query until the best-$k$
radius is certified, and evaluate distances only for candidates in
the visited bins. Prior axis-aligned GPU cell lists hit a hard
dimensional cap because the bin tensor scales as
$n_{\text{bins}}^d$; beyond $d_{\max}{=}5$, the bin array is
impractical and the implementation falls back to brute force. This is
the low-to-moderate-dimensional regime targeted here.
FastGraph's PCA step is therefore not a compression trick but a
change of binning coordinates: it reduces the search volume while
leaving every final distance comparison in the original space. That
separation is what lets the method inherit the pruning behavior of
cell lists without inheriting their axis-aligned dimensional cap.

FastGraph resolves this cap by binning in the top-$k$ principal
subspace while retaining full-dimensional distances for candidate
evaluation. Since the partial projection
$V_k V_k^\top \preceq I_d$ is contractive, projected-space
termination remains a valid lower bound on true-space distance, so
the algorithm is exact at every input dimension. The new PCA path is
evaluated at $d{=}4$--$10$; $d{=}2$--$3$ are axis-aligned controls for
which the implementation short-circuits before PCA. At $d{=}8$,
$N{=}5$\,M, $k{=}40$, FastGraph builds the kNN graph in
\fgcPCADeight\ versus \faissDeight\ for exact FAISS-GPU,
\cuvsBFDeight\ for cuVS brute force, and \cagraNNDDeight\ for
CAGRA's approximate NN-Descent build. Across $d{=}2$--$10$ at this
$(N,k)$ point, geomean speedups are \geoFaiss, \geoCuvs, and
\geoCagra\ respectively.

The library is shipped as a PyTorch extension. The axis-aligned primitive
remains callable from TorchScript, while the PCA wrapper uses eager-mode
\texttt{torch.pca\_lowrank}. Gradients flow through the continuous outputs of the kernel
(selected squared distances, query coordinates) while the hard
neighbor indices are treated as constants in the backward pass.
\texttt{GravNetOp}~\cite{Qasim2019GravNet} exposes PCA-subspace neighbor
selection through \texttt{use\_pca=True} in eager mode. A GPU-resident
object-condensation helper~\cite{Kieseler2020ObjectCondensation} is also
provided and described in the appendix.

\section{Related Work}
\label{sec:related-work}

\paragraph*{Exact GPU $k$NN.}
Modern GPUs make the inner-loop distance computation of exact $k$NN
embarrassingly parallel. FAISS~\cite{johnson2019billion} provides a
highly tuned \texttt{IndexFlatL2} exact brute-force GPU index together
with the fast $k$-selection primitives that downstream methods reuse.
RAPIDS \texttt{cuVS} exposes a comparable exact brute-force GPU
implementation that we use as the strongest exact baseline in this
work; KeOps~\cite{feydy2020fast} provides symbolic matrix reductions
with automatic differentiation that can express exact $k$NN without
materializing the full distance matrix. Direct exact GPU $k$NN-graph
construction has also been studied in the multi-GPU and cluster
settings~\cite{dashti2013efficient,komarov2014fast}. FastGraph is not
the first exact GPU $k$NN library; it specializes the accelerator for
the small-$d$ exact graph-construction setting that arises in dynamic
GNN layers.

\paragraph*{Cell lists and spatial binning.}
The cell list is the canonical low-dimensional accelerator: it dates
to Verlet's molecular-dynamics neighbor-finding scheme and underpins
modern MD libraries such as HOOMD-blue. The construction is the same
across communities---uniform grid in $d$ dimensions, sort points by
bin, expand a concentric hypercube of bins around each query until
the best-$k$ radius is certified---but the choice of $d$ has been
constrained in practice to two or three axis-aligned dimensions
because the bin tensor scales as $n_{\text{bins}}^d$.
Qasim's PhD thesis~\cite{qasim2023thesis} discusses this cap
explicitly in the GNN context; FastGraph's PCA-subspace formulation
lifts it.

\paragraph*{PCA-projected indexing.}
Using a principal-component projection as a coordinate system for
spatial indexing has a long history; Sproull's $k$-d tree
refinements~\cite{sproull1991refinements} already noted that
splitting on the direction of maximum variance yields more balanced
partitions than splitting on a coordinate axis. PCA trees and
random-projection trees use a similar idea for CPU-side approximate
nearest-neighbor search. FastGraph's PCA-subspace binning differs in
two ways: the projection is partial (top-$k$ axes) rather than full,
and the candidate-distance evaluation that follows the pruning step is
performed in the \emph{original} $d$-dimensional space, preserving
exactness. The contraction $V_k V_k^\top \preceq I_d$ is what makes
that combination valid (Section~\ref{sec:method}).

\paragraph*{Three-dimensional spatial-acceleration $k$NN.}
A separate line of work targets exact GPU $k$NN through dedicated
spatial-acceleration structures that exploit the GPU's
ray-tracing hardware or BVH machinery. RTNN~\cite{wald2019rtnn},
RT-kNNS Unbound~\cite{evangelou2021rtknn}, and
Arkade~\cite{nagarajan2023arkade} retarget the OptiX BVH unit for
neighbor search and report large speedups over shader-core baselines
in 3D. LBVH-kNN~\cite{jakob2021optimized} (\texttt{cupy-knn}) attains
similar gains via a left-balanced LBVH built without dedicated
RT hardware. CLOVER~\cite{kamel2025clover} introduces a GPU-native,
Voronoi spatio-graph approach to exact $k$NN and reports
$4\times$ over an ``optimized grid'' baseline and
$230\times$ over FAISS on SIFT-like data. All of these methods are
fundamentally locked to two or three input dimensions: Arkade notes
that RT cores build BVHs only on three-dimensional data, CLOVER's
reference implementation asserts \texttt{D == 3}, and LBVH-kNN is
evaluated exclusively in 3D. We include a direct head-to-head against
CLOVER at $d{=}3$ in Section~\ref{sec:clover}, but these methods do
not cover the $d{=}4$--$10$ latent-space workload targeted here.

\paragraph*{Approximate GPU $k$NN.}
A parallel body of work has produced GPU-resident approximate nearest
neighbor (ANN) systems. SONG~\cite{zhao2020song} decomposes graph
traversal into GPU-friendly stages; GGNN~\cite{ggnn} builds a
hierarchical $k$NN graph for index construction and search;
CAGRA~\cite{ootomo2023cagra}, distributed as part of RAPIDS
\texttt{cuVS}, builds a high-quality graph index with an IVF-PQ or
NN-Descent initialization. ANN-Descent variants have also been adapted
to reduce memory traffic during construction~\cite{wang2021large}.
These systems target high-throughput vector retrieval at recall
$\lesssim 0.9$; FastGraph occupies the orthogonal exact, small-$d$,
graph-construction regime where the cost of an approximate edge is
not borne by a single retrieval call but accumulates across every
training event. We benchmark CAGRA and GGNN as approximate
comparators (Section~\ref{sec:recall}) but do not include CPU-only ANN
systems (HNSW, ScaNN, Annoy)~\cite{malkov2020hnsw,guo2020scann,bernhardsson2013annoy}
in the timing tables: the workload here is GPU-resident end-to-end
and a CPU-side detour would not be a fair comparator.

\paragraph*{Dynamic latent-graph models in geometric deep learning.}
Dynamic kNN-graph layers in learned latent spaces are a recurring
ingredient in geometric deep learning. DGCNN/EdgeConv recomputes the
graph in feature space at each layer~\cite{wang2019dynamic};
ParticleNet adapts the same idea to jet
tagging~\cite{qu2020particlenet}; GravNet/GarNet
layers~\cite{Qasim2019GravNet} and the object condensation
loss~\cite{Kieseler2020ObjectCondensation} are deployed for HGCAL
reconstruction~\cite{qasim2021multiparticle,bhattacharya2022gnn}; the
HEP.TrkX/Exa.TrkX line of work uses learned-embedding graphs for
tracking~\cite{ju2021performance,lazar2022accelerating,choma2020track}.
FastGraph is best framed as an exact substrate for the kNN-graph layer
inside such models---not a new model class.

\section{The PCA-Subspace Binned $k$NN Algorithm}
\label{sec:method}

\subsection{Phase 1: Subspace estimation}
\label{sec:phase1}

Given the input coordinates
$X \in \mathbb{R}^{N \times d}$ for the current call, FastGraph samples
up to 50,000 rows, centers that sample, and computes the top-$k$ principal
axes with \texttt{torch.pca\_lowrank}. The current release fits one
projection for the complete coordinate tensor rather than a separate
projection for each \texttt{row\_splits} segment. The number of
binning axes $k$ is a user-facing hyperparameter; we default to
$k = \mbdPCAdefault$ for the HGCAL workloads in this paper, justified
empirically in the ablation of Section~\ref{sec:mbd-ablation}. The
resulting projection matrix $V_k \in \mathbb{R}^{d \times k}$
satisfies $V_k^\top V_k = I_k$ and $V_k V_k^\top \preceq I_d$. The
projected coordinates used for binning are $Y = X V_k$. PCA estimation
is performed once per call on the GPU and is included in the reported
FastGraph timing. The random subsample follows PyTorch's active random
number-generator state.

\paragraph*{Short-circuit at $d \le k$.}
When the input dimensionality $d$ does not exceed the binning
dimensionality $k$, FastGraph skips PCA estimation entirely and
binds the binning axes to the raw input axes; the projection
$V_k$ reduces to the identity (up to permutation), $Y = X$, and the
algorithm behaves exactly as a classical axis-aligned cell list. With
the default $k = \mbdPCAdefault$, the $d \in \{2, 3\}$ gains therefore
come from the underlying cell-list machinery; the PCA contribution
begins at $d > k$, which covers the $d{=}4$--$10$ HGCAL latent-space
range.

\subsection{Phase 2: Binning in the projected subspace}
\label{sec:phase2}

We partition $Y$ with a uniform grid of width $w$ along each of the
$k$ projected axes. The per-axis division count is set heuristically
to balance bin density against neighborhood reach,
\[
  n_{\text{bins}} = \left(\frac{32\,n_{\text{elems}}}{K}\right)^{\frac{1}{k}},
\]
where $n_{\text{elems}}$ is the per-row-split point count, $K$ is the
requested neighborhood size, and the result is clamped to
$[5,30]$. Points are sorted by bin index, and cumulative offsets are
stored so that each bin can be addressed as a contiguous slice of the
sorted point array. Row splits, which delineate the per-event
boundaries inside a batched tensor, are honored by the binning kernel
so that no query bin spans events.

\subsection{Phase 3: Per-query search with full-dim distances}
\label{sec:phase3}

For each query $x_q$, the search starts in the bin containing
$y_q = x_q V_k$ and expands through successive shells of $k$-D
hypercube bins. For every candidate $u$ in a visited bin the
\emph{full-dimensional} squared Euclidean distance
$\|x_q - x_u\|^2 = \sum_{i=1}^{d}(x_{q,i} - x_{u,i})^2$ is computed
(not the $k$-dimensional projected distance). The current best
neighbor radius $r_K$ and the heap of nearest-$K$ candidates are
maintained incrementally.

\subsection{Why this is exact: the contraction argument}
\label{sec:exactness}

For any dataset size $N$, ambient dimension $d$, projection dimension
$k \le d$, and neighbor count $K$, consider the standard cube-radius
termination test applied to bins in the projected subspace while all
candidate distances are evaluated in the full input space. The
exactness of this test rests on a single observation about the partial
projection. For any two points $x_a, x_b \in \mathbb{R}^d$,
\[
  \|y_a - y_b\|^2
  = (x_a - x_b)^\top V_k V_k^\top (x_a - x_b)
  \le \|x_a - x_b\|^2,
\]
because $V_k V_k^\top$ has eigenvalues in $\{0,1\}$ and is therefore
$\preceq I_d$. Equivalently, the projection is contractive: the
projected distance never overestimates the true distance.

After visiting all bins within the current hypercube shell of radius
$s$, any unvisited bin lies at projected distance at least
$w_{\min} \cdot s$ from $y_q$, where
$w_{\min} = \min_j \texttt{binWidths}[j]$ is the most conservative bin
width across the projected axes. Thus any unvisited point satisfies
$\|y_q - y_u\|^2 \ge (w_{\min} \cdot s)^2$. Combined with the
contraction above, $\|x_q - x_u\|^2 \ge (w_{\min} \cdot s)^2$, so
once the heap has filled and $(w_{\min} \cdot s)^2 > r_K^2$, no unseen
point can be closer in the true distance than the current $K$-th
neighbor. The algorithm may safely terminate; the returned $K$
neighbors are exact under the paper's distance convention. We
additionally verify exactness empirically against brute force at
tractable scale using the tie-aware distance convention of
Section~\ref{sec:recall}, obtaining recall $=1.0$ and zero distance
discrepancies across all spot-checked configurations.

\subsection{Binstepper}

The \texttt{binstepper} is the small bookkeeping utility that
enumerates the surface of the $s$-th hypercube shell around the
query bin. It linearises each shell into a flat sequence, converts
each step back into per-dimension offsets, discards interior cells,
and yields the next surface bin's flat index. The stepper carries no
algorithmic role; the kNN guarantee lives in the outer kernel of
Algorithm~\ref{alg:knn}. Its job is purely to enumerate the projected
shells in a cache-friendly order so the distance kernel can stay
focused on the full-dimensional checks. Keeping the shell enumerator
separate from the distance kernel also makes the projected walk
event-local: the same row-split offsets and contiguous bin slices can
be reused across all queries in the batch.

\subsection{Pseudocode}

Algorithm~\ref{alg:knn} makes the projected-vs-original-space
distinction explicit: the bin indices (\texttt{binIdx}) and the
stepper operate on $y$-space; the distance function
\texttt{calcDist} operates on the original $x$-space coordinates;
the termination test references the projected-space cube radius;
the contraction argument certifies the result. This separation keeps
the implementation and the proof aligned: projected-space search only
decides which bins to visit, while exactness is enforced by the
original-space distance computation.

\begin{algorithm}[H]
\DontPrintSemicolon
\KwIn{\texttt{coords}[$n_v$][$n_c$] (original-space $X$), $V_k$, \texttt{binIdx}[$n_v$] (projected-space bins), \texttt{binOffsets}[$n_v$][$n_b{+}1$], \texttt{binBounds}[$n_B$], \texttt{binCounts}[$n_b$], \texttt{binWidths}[$n_b$], $n_v,K,n_c,n_b,n_B$}
\KwOut{\texttt{neighbors}[$n_v$][$K$] indices including slot 0 self by convention, $r_K^2$ values}
\ForEach{$v \in [0,n_v)$ \textbf{in parallel}}{
  \texttt{neighbors}[v][0]$\leftarrow(v,0)$; \texttt{filled}$\leftarrow1$; $(\texttt{maxIdx},\texttt{maxD2})\leftarrow(0,0)$\;
  $\texttt{totalBins}\leftarrow\prod_{d=0}^{n_b-1}\texttt{binCounts}[d]$, $\texttt{gOff}\leftarrow \texttt{totalBins}\cdot\lfloor \texttt{binIdx}[v]/\texttt{totalBins}\rfloor$\;
  \texttt{step}$\leftarrow$\texttt{BinStepper}(\texttt{binCounts}, \texttt{binOffsets}[v]$[1..n_b]$) \tcp*{operates in projected $y$-space}
  \texttt{wMin}$\leftarrow \min_j \texttt{binWidths}[j]$\;
  \texttt{cont}$\leftarrow$ true; $s\leftarrow0$ \tcp*{$s$: current cube-shell radius (projected space)}
  \While{\texttt{cont}}{
    \texttt{step.setDistance}($s$); \texttt{cont}$\leftarrow$ false\;
    \While{$(\mathtt{off} \leftarrow \mathtt{step.step()}) \neq -1$}{
      $\texttt{idx}\leftarrow \texttt{off}+\texttt{gOff}$\;
      \If{$\texttt{idx}<0$ \textbf{or} $\texttt{idx}\ge n_B-1$}{\Continue}
      $(s_b,e_b)\leftarrow(\texttt{binBounds}[\texttt{idx}],\texttt{binBounds}[\texttt{idx}{+}1])$\;
      \If{$s_b=e_b$}{\texttt{cont}$\leftarrow$ true; \Continue}
      \For{$u\in[s_b,e_b)$}{
        \If{$u{=}v$}{\Continue}
        $\texttt{d2}\leftarrow \texttt{calcDist}(v,u)$ \tcp*{full $d$-dim $\sum_i(x_{v,i}-x_{u,i})^2$, NOT projected}
        \If{$\texttt{filled}<K$}{\texttt{neighbors}[v][\texttt{filled}]$\leftarrow(u,\texttt{d2})$;\ \If{$\texttt{d2}>\texttt{maxD2}$}{$(\texttt{maxIdx},\texttt{maxD2})\leftarrow(\texttt{filled},\texttt{d2})$}; \texttt{filled}++}
        \ElseIf{$\texttt{d2}<\texttt{maxD2}$}{\texttt{neighbors}[v][\texttt{maxIdx}]$\leftarrow(u,\texttt{d2})$;\ $(\texttt{maxIdx},\texttt{maxD2})\leftarrow\texttt{findMaxDist}(\texttt{neighbors}[v])$}
      }
      \texttt{cont}$\leftarrow$ true
    }
    \If{$\texttt{filled}{=}K$ \textbf{and} $(\texttt{wMin}\cdot s)^2>\texttt{maxD2}$}{\Break}
    $s$++
  }
}
\caption{\textbf{PCA-Subspace Binned-Select-kNN}: bins live in projected $y$-space; distances are computed in original $x$-space. Slot 0 is the query point itself, matching the FastGraph tensor convention; downstream message-passing code may drop or ignore this self edge.}
\label{alg:knn}
\end{algorithm}

\vspace{-0.6em}
\subsection{Complexity}

Let $B = n_{\text{bins}}^{k}$ be the size of the bin grid. The PCA
projection reduces this from the $n_{\text{bins}}^{d}$ of an
axis-aligned scheme to a quantity that depends only on the binning
subspace size $k$, \emph{independently of the input dimension} $d$.
This is what removes the small-$d$ cap that limited prior cell-list
GPU $k$NN implementations.

\noindent\textit{Setup.} Subspace estimation runs a partial
eigen-decomposition of $X^\top X$ at $O(N d^2 + d^3)$, dominated in
our regime by the $N d^2$ Gram accumulation. Binning, sorting, and
offset construction take $O(N \log N + B)$.

\noindent\textit{Memory.} Auxiliary memory is $O(N(d + k + K) + B)$:
sorted coordinates, projected coordinates, permutation, neighbor
indices, and bin offsets.

\noindent\textit{Per-query search.} Under roughly uniform
projected-space density, each query examines $O(K)$ useful candidates
before certifying $r_K$; each candidate costs $O(d)$ for the full-dim
distance and at most $O(K)$ for the heap update, giving expected
$O(K(d + K))$ per query. The worst case is the degenerate
all-points-in-one-bin configuration, which falls back to the
brute-force $O(N(d + K))$. In the dynamic-graph setting, the setup
terms are amortized over the full query sweep on each event, so the
per-query behavior is the more important operational quantity.

\noindent\textit{Parallel cost.} With $P$ CUDA threads,
\[
  \frac{N}{P} \cdot O\!\left(K(d + K)\right)
  \;+\; O\!\left(N d^2 + N \log N + B\right).
\]

\subsection{Gradient flow and PyTorch integration}

FastGraph exposes gradients through the continuous outputs of the
kernel---the selected squared distances and the input coordinates---
while treating the discrete neighbor indices and the projection
matrix $V_k$ as constants in the backward pass. Concretely, the
forward returns $(\text{idx}, \text{dist}^2)$ and the backward
applies the chain rule with respect to $\text{dist}^2$ and the input
coordinates, yielding a subgradient of the piecewise-smooth loss
induced by the hard neighbor selection. This is the same gradient
model used by any fixed-index distance layer
(e.g.,\ \texttt{torch.cdist} followed by \texttt{topk}) and integrates
naturally into existing GNN autograd graphs. Treating $V_k$ as
fixed per forward+backward pass keeps the gradients deterministic
within a step; the projection is refreshed at the next forward.

The PCA entry point is an eager-mode Python wrapper around the scripted
axis-aligned primitive and the custom CUDA operation. A minimal usage
example is:

\begin{lstlisting}[language=Python, caption={Minimal eager-mode PCA-FastGraph usage.}]
from fastgraphcompute import binned_select_knn_pca
from fastgraphcompute.gnn_ops import GravNetOp

# Direct call (eager mode)
idx, dist2 = binned_select_knn_pca(K=40, coords=x, row_splits=rs,
                                   max_bin_dims=3)
# idx:   (N, K) int64 neighbor indices (constant in backward)
# dist2: (N, K) float32 squared distances (differentiable)
loss = model(dist2).sum()
loss.backward()  # gradients flow through dist2 to x

# Opt-in PCA neighbor construction inside GravNetOp (eager mode)
layer = GravNetOp(in_channels=16, out_channels=32,
                  space_dimensions=8, propagate_dimensions=32,
                  k=40, use_pca=True, max_bin_dims=3)
out, idx, dist2, space = layer(features, rs)
\end{lstlisting}

\section{Experimental Setup}
\label{sec:expsetup}

All GPU experiments use a single NVIDIA A100 80\,GB PCIe with CUDA
12.1 and PyTorch 2.5. Compared backends are exact FAISS-GPU
\texttt{IndexFlatL2}~\cite{johnson2019billion}, cuVS brute force
(also exact), CAGRA via cuVS~\cite{ootomo2023cagra}
(approximate), and GGNN~\cite{ggnn} (approximate); FastGraph is
compiled from the public release.

\paragraph*{Timing protocol.}
The benchmark archive contains three Slurm allocation modes. PCA-FastGraph
and its cross-allocation calibration cells were collected with
\texttt{--gres=mps:50}; the later full CAGRA-NN-Descent and GGNN sweeps
used \texttt{--gres=mps:100}; and some legacy exact-baseline cells used
an exclusive \texttt{--gres=gpu:1} allocation. Jobs used for latency
comparisons were serialized to avoid MPS contention, and calibration
cells were used to identify allocation-sensitive or contaminated runs.
All timed regions are bracketed by device synchronization. The FastGraph
timer receives GPU-resident coordinates, whereas the FAISS, cuVS, and
CAGRA runners begin with NumPy input and include device conversion, index
construction, and search. Reported values are therefore runner-level graph
construction times rather than kernel-only measurements. Figures and
scalar comparisons report medians across successful repetitions; error
bars show the interquartile range when repeated measurements are available.

\paragraph*{CAGRA build path.}
CAGRA's default IVF-PQ build path fails on HGCAL data at $d \ge 5$
with a RAFT assertion about invalid or duplicated neighbor nodes. The
failure is consistent with PQ codebook collapse on heterogeneous-scale
features: energy, geometry, time, and layer index occupy very different
numeric ranges. We therefore use CAGRA's \texttt{build\_algo="nn\_descent"}
path throughout the paper; it avoids the PQ initialization, succeeds
across the full $d{=}2$--$10$ sweep, and is the robust full-sweep
CAGRA baseline used in all plots and tables.

\paragraph*{The \texttt{max\_bin\_dims} hyperparameter.}
The binning subspace size $k$ is exposed as
\texttt{max\_bin\_dims}. The released CUDA implementation provides
compile-time kernel template instantiations for
$k \in \{2, 3, 4, 5\}$; we use $k = \mbdPCAdefault$ as the
default for HGCAL and ablate the released kernel surface in
Section~\ref{sec:mbd-ablation}. Development-only $k=6,7$ kernels
were used to probe the boundary of the design, but are not part of
the paper-release branch; $k=7$ exceeds the current per-block
register budget. The empirical optimum on HGCAL data is well inside
the released range, so this is not a binding limitation in practice.

\paragraph*{Benchmark data.}
The CMS HGCAL recHit feature dataset comprises \hgcalRecHits\ hits
concatenated across all events with up to ten spatial and energy
features per hit. Synthetic comparisons use isotropic Gaussian
$\mathcal{N}(0, I_d)$ samples; this matches the benchmark protocol
used in this paper and isolates the effect of dimensionality from the
heterogeneous-scale pathology of HGCAL.

\section{Performance Comparisons}

\subsection{Headline benchmark: Dimensional scaling on detector data}
\label{sec:headline}

\begin{figure}[!htbp]
\centering
  \begin{minipage}[c]{0.56\textwidth}
  \centering
  \includegraphics[width=\linewidth]{gpu_fgc_dimensional_scaling_5M_k40_all_algorithms.png}
  \end{minipage}
  \hfill
  \begin{minipage}[c]{0.42\textwidth}
  \centering
  \scriptsize
  \setlength{\tabcolsep}{1.8pt}
  \begin{tabular}{ccccc}
  \toprule
  $d$ & FAISS & cuVS & CAGRA & GGNN \\
  \midrule
  2  & 3.42$\times$  & 1.65$\times$  & 1.45$\times$  & 0.32$\times$ \\
  3  & 2.96$\times$  & 1.43$\times$  & 1.28$\times$  & 0.28$\times$ \\
  4  & 2.30$\times$  & 1.12$\times$  & 0.98$\times$  & 0.22$\times$ \\
  5  & 3.09$\times$  & 1.48$\times$  & 1.39$\times$  & 0.28$\times$ \\
  6  & 10.51$\times$ & 4.92$\times$  & 4.40$\times$  & 0.49$\times$ \\
  7  & 35.23$\times$ & 16.77$\times$ & 14.53$\times$ & 1.48$\times$ \\
  8  & 42.46$\times$ & 20.15$\times$ & 17.36$\times$ & 1.86$\times$ \\
  9  & 32.68$\times$ & 15.60$\times$ & 13.37$\times$ & 1.42$\times$ \\
  10 & 29.83$\times$ & 14.31$\times$ & 13.67$\times$ & 1.48$\times$ \\
  \midrule
  geo. & 10.1$\times$ & 4.8$\times$ & 4.3$\times$ & 0.63$\times$ \\
  \bottomrule
  \end{tabular}
  \end{minipage}
  \caption{Headline benchmark on detector data at $N{=}5$\,M, $k{=}40$.
  Left: dimensional scaling of wall-clock time. FastGraph is the
  fastest exact GPU method across the full $d{=}2$--$10$ range.
  Right: speedups of FastGraph over each backend at the same operating
  point. FAISS and cuVS BF are exact; CAGRA-nnd and GGNN are approximate
  default-recall baselines, and GGNN's timing should be read with its
  substantially lower recall in this regime (Section~\ref{sec:recall}).}
  \label{fig:headline_dim}
\end{figure}

The right-hand table in Figure~\ref{fig:headline_dim} reports the cross-method speedups
of FastGraph at the headline operating point. FastGraph is the
fastest \emph{exact} GPU $k$NN method at every dimension we measure
and has a \geoCagra\ geomean advantage over the approximate
CAGRA-NN-Descent build. The GGNN approximate baseline does win on raw
wall-clock at low $d$, but its much lower recall scores in matched-
recall evaluation (Section~\ref{sec:recall}) mean the timing should be
read with that context in mind. The relative advantage is widest in the
$d{=}7$--$10$ regime where dense exact baselines pay the full
quadratic cost and the approximate baselines have to retain enough
graph quality to remain useful. Geomean across $d{=}2$--$10$ at this
operating point is \geoFaiss\ over FAISS, \geoCuvs\ over cuVS BF
(the strongest exact GPU baseline), and \geoCagra\ over
CAGRA-NN-Descent. Section~\ref{sec:mbd-ablation} separately reports
the internal axis-aligned ablation that motivated the PCA-subspace
design; the headline table therefore reports only the PCA-subspace
method.

The qualitative shape of the curve is determined by the regime
transition at $d{=}5$--$6$. Below this transition, all GPU methods
are within a small constant factor and the binning advantage is
modest; above it, FAISS/cuVS BF (which scale as $N^2 d$) both grow
rapidly with $d$, while FastGraph retains a useful pruning structure
through PCA-subspace binning.

\subsection{Dataset-size scaling}

Figure~\ref{fig:size_scaling} reports dataset-size scaling at fixed
$k{=}40$ for $d{=}3$ and $d{=}8$; the same ordering persists as $N$
grows, with stronger separation in the PCA-favorable regime.

\begin{figure}[!t]
\centering
  \begin{minipage}{0.49\textwidth}
  \centering
  \includegraphics[width=\linewidth]{gpu_fgc_speedup_d3_all_algorithms.png}
  \end{minipage}
  \hfill
  \begin{minipage}{0.49\textwidth}
  \centering
  \includegraphics[width=\linewidth]{gpu_fgc_speedup_d8_gpu.png}
  \end{minipage}
  \caption{Dataset-size scaling on HGCAL at $k{=}40$ for $d{=}3$
  (left) and $d{=}8$ (right). FastGraph maintains a speed advantage
  over exact baselines most clearly in the higher-dimensional
  PCA-favorable regime.}
  \label{fig:size_scaling}
\end{figure}

Figure~\ref{fig:k_comparison} checks that the headline dimensional
trend is not an artifact of the single $k{=}40$ operating point.
Across $k\in\{10,40,100\}$, the low-dimensional constant-factor
regime and the larger $d{\geq}6$ speedups remain visible.

\begin{figure}[!htbp]
\centering
  \includegraphics[width=0.95\textwidth]{gpu_fgc_k_comparison_1M_d2-10_all_algorithms.png}
  \caption{Wall-clock at $N{=}1$\,M for $k\in\{10, 40, 100\}$ across
  the full dimensional range. The qualitative pattern of
  Figure~\ref{fig:headline_dim} is preserved across $k$.}
  \label{fig:k_comparison}
\end{figure}

\subsection{Controlled synthetic benchmarks}
\label{sec:synthetic}

On isotropic Gaussian $\mathcal{N}(0, I_d)$ data, FastGraph wins
decisively at low dimensions, while dense brute-force GPU matmul
(cuVS BF) becomes competitive as $d$ grows. This is consistent with the
algorithmic interpretation: the PCA step has no anisotropy to exploit
on isotropic data, so the gain comes from ordinary cell-list pruning
rather than from a favorable learned basis. At low $d$, cell-list
pruning still dominates the dense $\mathcal{O}(N^2 d)$ matmul of
the exact GPU baselines, giving FastGraph a $\spdSynthDthree$
speedup over cuVS BF at $d{=}3$ and a $\spdSynthDfive$ speedup at
$d{=}5$ (Table~\ref{tab:synth-dim}). As $d$ grows the number of
neighboring cells grows combinatorially, the binning savings
collapse, and dense matmul on a well-tuned BLAS path is close to
optimal on isotropic data. Figure~\ref{fig:synth_summary} shows the
full dimensional sweep and two dataset-size scans at $d{=}3$ and $d{=}8$.

\begin{figure}[!htbp]
\centering
  \includegraphics[width=0.95\textwidth]{synth_summary_gpu.png}
  \caption{Controlled synthetic Gaussian benchmarks. Top:
  dimensional scaling at $N{=}1$\,M, $k{=}40$. Bottom:
  dataset-size scaling at $d{=}3$ and $d{=}8$, $k{=}40$.}
  \label{fig:synth_summary}
\end{figure}

\begin{table}[!htbp]
\centering
\caption{FastGraph speedup over GPU baselines on synthetic Gaussian
$\mathcal{N}(0, I_d)$ data at $N{=}10^6$, $k{=}40$ (median of
3 reps on the fixed-seed synthetic Gaussian benchmark set used
throughout this paper). The HGCAL detector-data wins at $d \geq 6$
(Figure~\ref{fig:headline_dim}) are not reproduced on isotropic
data, isolating them to the anisotropic structure of detector
features rather than a generic property of cell-list binning.}
\label{tab:synth-dim}
\begin{tabular}{lrrr}
\toprule
$d$ & vs cuVS BF & vs FAISS-GPU & vs CAGRA-nnd \\
\midrule
3 & 55.2$\times$ & 108.6$\times$ & 205.8$\times$ \\
5 &  4.8$\times$ &   9.4$\times$ &  18.0$\times$ \\
7 &  0.8$\times$ &   1.7$\times$ &   3.2$\times$ \\
\bottomrule
\end{tabular}
\end{table}

The implication for the headline claim is that the HGCAL detector
wins at $d{=}6$--$10$ (Section~\ref{sec:headline}) come from PCA
picking up anisotropic structure that does not exist in isotropic
synthetic data, not from cell-list binning alone.

\subsection{Memory footprint}
\label{sec:memory}

\begin{figure}[!htbp]
\centering
  \includegraphics[width=0.95\textwidth]{memory_footprint.png}
  \caption{GPU memory footprint per backend at $d{=}3$, $k{=}40$,
  measured by a 100\,Hz NVML polling thread that captures the true
  peak during the call. \emph{Left:} workspace, defined as
  $\text{peak} - N{\times}k{\times}(8{+}4)$ bytes, i.e.\ everything
  the algorithm allocates beyond the returned $(\text{idx},
  \text{dist})$ tensors, with \texttt{idx} stored as int64 and
  \texttt{dist} as float32; this isolates temporary algorithm
  workspace from the common output allocation.
  \emph{Right:} end-state, the resident NVML reading after the call
  synchronises while the caller still holds the output tensors.}
  \label{fig:memory_footprint}
\end{figure}

\paragraph*{Workspace and resident memory.}
FastGraph allocates more workspace than the dense GPU baselines: at
$N{=}5$\,M, $d{=}3$, $k{=}40$ it uses about $3.0$\,GB versus
$1.4$\,GB for FAISS-GPU, CAGRA-nn-descent, and GGNN, and
$\sim 80$\,MB for cuVS BF; the corresponding FastGraph workspace at
$d{=}8$ is $\sim 3.3$\,GB. This cost comes from the projected
coordinate array, bin assignments and boundaries, sort/remap
intermediates, and tensors required by the differentiable distance
outputs. In resident end-state memory, the gap is smaller:
FastGraph holds $\sim 5.3$\,GB versus $1.6$--$2.9$\,GB for the other
backends, because the common $N{\times}k$ output tensors dominate
resident memory after temporary workspace is released.

\paragraph*{Approximate-backend interpretation.}
The plot reports each backend at the configuration used elsewhere in
this paper. FAISS-GPU \texttt{IndexFlatL2} is exact; cuVS BF is
exact. CAGRA-nn-descent and GGNN are approximate at their default
quality settings; their memory advantage is partly inherited from
running at lower effective recall (see
Section~\ref{sec:recall}). A matched-recall memory comparison would
require either raising their quality parameters until recall hits a
target or downsampling their internal graphs, neither of which has a
canonical setting; we therefore report the memory each backend uses
\emph{as benchmarked}, with the understanding that the approximate
backends are not bound to FastGraph's exact-recall constraint.

\paragraph*{Measurement methodology.}
Memory is measured with a background NVML poller running at
$\sim 100$\,Hz during the timed region. The reported workspace is the
observed peak minus the backend's output-tensor footprint, while the
end-state value is the resident allocation after synchronization with
the output tensors still live.

\subsection{Binning dimensionality ablation}
\label{sec:mbd-ablation}

The binning subspace size $k$ (\texttt{max\_bin\_dims}) is the most
important FastGraph hyperparameter. We set the default to
$k{=}\mbdPCAdefault$ because it is the fastest PCA-subspace setting at
the reference cell $d{=}8$, $N{=}5$\,M, $k_{\text{nn}}{=}40$.
Table~\ref{tab:mbd-ablation} compares this choice with the
axis-aligned cell list over the released kernel surface
$k \in \{2,3,4,5\}$, using medians over three repetitions.

\begin{table}[!htbp]
\centering
\caption{Effect of the binning subspace size $k$ on FastGraph
wall-clock at $d{=}8$, $N{=}5$\,M, $k_{\text{nn}}{=}40$, median of
three repetitions. The axis-aligned variant's optimum within
the released kernel surface is $k{=}\mbdOptAxis$; the PCA-subspace
variant's runtime optimum, and therefore the default used in the
paper, is $k{=}\mbdOptPCA$.}
\label{tab:mbd-ablation}
\small
\begin{tabular}{cccc}
\toprule
$k$ (bin dims) & axis (s) & PCA (s) & PCA/axis speedup \\
\midrule
2 & 199.37 & 11.39 & 17.5$\times$ \\
3 & 204.65 &  \textbf{7.48} & \textbf{27.4}$\times$ \\
4 & 353.78 & 11.58 & 30.6$\times$ \\
5 & 242.90 & 28.10 &  8.6$\times$ \\
\bottomrule
\end{tabular}
\end{table}

Both variants are non-monotonic in $k$, but their optima differ: the
axis-aligned cell list is fastest at $k{=}\mbdOptAxis$, while the
PCA-subspace variant is fastest at $k{=}\mbdOptPCA$. The largest
matched-$k$ speedup occurs at $k{=}4$, but $k{=}3$ is the setting used
by FastGraph because it minimizes the PCA-subspace wall-clock. Across
the sweep, PCA-subspace binning is $8.6$--$30.6\times$ faster at
matched $k$, and comparing each variant at its own optimum gives a
\fgcVanillaBestPCAfactor\ speedup.

We expose $k$ as a user-facing hyperparameter rather than hard-coding
it: the optimum is data-dependent (a dataset with intrinsic
dimensionality near 2--3 prefers small $k$; one with variance spread
more uniformly prefers $k{=}4$ or $5$), and the same kernel binary
supports all four instantiations. The default
$k{=}\mbdPCAdefault$ is HGCAL-tuned and should be revalidated when
deploying FastGraph on datasets with different intrinsic dimension.

\subsection{Three-dimensional baseline: CLOVER head-to-head}
\label{sec:clover}

CLOVER~\cite{kamel2025clover} is the strongest among the 3D-only
exact-GPU $k$NN methods surveyed in Section~\ref{sec:related-work}
and the only method in this group that our head-to-head shows beating
FastGraph on raw 3D detector coordinates. This comparison does not
weaken the headline claim: CLOVER is restricted to raw 3D point
clouds, whereas FastGraph targets exact $k$NN in learned latent
spaces with $d{=}4$--$10$.

\begin{figure}[!htbp]
\centering
  \includegraphics[width=0.9\textwidth]{clover_headtohead_d3.png}
  \caption{FastGraph vs CLOVER at $d{=}3$, $k{=}40$, across three
  data distributions: HGCAL recHits (left), synthetic Gaussian
  $\mathcal{N}(0, I_3)$ (center), synthetic uniform on $[0,1]^3$
  (right). CLOVER is only implemented for $D{=}3$, so $d{=}3$ is the
  only dimensionality at which a direct comparison is possible.}
  \label{fig:clover_d3}
\end{figure}

The result splits along the structure of the input distribution.
On uniform-cube data --- the regime where a uniform-grid cell list is
near-optimal --- FastGraph is 5--8$\times$ faster than CLOVER hubs.
On isotropic Gaussian data the two methods are within roughly a
factor of two. On anisotropic HGCAL recHits, CLOVER's hubs index is
much faster at $d{=}3$: with the default $k_{\rm bin}{=}3$,
FastGraph short-circuits to the ordinary axis-aligned cell-list path,
while CLOVER uses a 3D density-adaptive spatio-graph. This is a
favorable regime for CLOVER and an intentionally out-of-scope regime
for FastGraph's PCA-subspace contribution.

The deployment regime for HGCAL graph-construction GNNs is the
\emph{learned latent space} of the trained network, not the raw 3D
detector coordinates. Modern dynamic-graph layers used in HEP
reconstruction operate at $d=4$--$10$ in this latent space:
GravNet uses $d=4$ in its original
formulation~\cite{Qasim2019GravNet} and $d \in \{6, 8, 10\}$ in
recent HGCAL multi-particle
variants~\cite{qasim2021multiparticle,bhattacharya2022gnn};
EdgeConv-derived layers~\cite{wang2019dynamic} and object
condensation pipelines~\cite{Kieseler2020ObjectCondensation} expose
similar ranges. CLOVER, LBVH-kNN, and the RT-core methods are
unavailable by construction in this regime: they are
hardware-locked or implementation-locked to $D=3$ and have no
defined extension to $d \geq 4$.

\subsection{Recall evaluation}
\label{sec:recall}

To verify FastGraph's exactness we compare against a brute-force
reference on HGCAL data up to $N{=}5\times 10^5$. Because many
recHits are spatially coincident, neighbor indices are not unique;
we therefore use \emph{distance-based recall}, counting a predicted
neighbor as correct when its squared distance is no larger than the
$k$-th reference distance plus a small tolerance. FastGraph and its
reference use element-wise squared $\ell_2$ distances, which avoid
the cancellation that can affect coincident-hit cases under the BLAS
expansion. FAISS-GPU \texttt{IndexFlatL2} is treated as exact under
its own L2 kernel; the recall plots report exact dense baselines under
their kernel-matched distance convention and approximate graph
builders under the same distance-based criterion used for their
returned neighbors.
In this setting the exactness claim is about the returned neighbor
set under a fixed distance convention, not about a unique graph
topology when ties are present.

Under this metric FastGraph achieves recall $= 1.0$ across every
tested configuration ($d \in \{2,\ldots,10\}$, $k_{\text{nn}} \in
\{10, 40, 100\}$, $N$ up to $5 \times 10^5$). The approximate
baselines, even at their highest-quality parameter settings, do not
reach exact recall. GGNN is the clearest example: at $d{=}5$ its
highest-quality setting collapses to roughly $0.03$ recall before
recovering at higher dimensions. Table~\ref{tab:recall} summarises
the mean and representative-cell values.

\begin{table}[!htbp]
\centering
\caption{Distance-based recall on HGCAL data at the highest-quality
parameter setting for each backend (\texttt{itopk\_size}$=512$ for
CAGRA-NN-Descent, $\tau_{\text{query}}=0.8$ for GGNN), under the
kernel-matched evaluation described in the text. The representative
cell is $d{=}3$, $N{=}5\times 10^5$, $k_{\text{nn}}{=}40$.}
\label{tab:recall}
\small
\begin{tabular}{l|cc}
\toprule
Method & mean over all cells & representative cell \\
\midrule
FastGraph (exact)   & $1.000$ & $1.000$ \\
CAGRA-NN-Descent    & $0.732$ & $0.818$ \\
GGNN                & $0.666$ & $0.410$ \\
\bottomrule
\end{tabular}
\end{table}

Two points are most relevant. First, neither approximate method
achieves $99\%$ distance-based recall at $N \ge 5 \times 10^5$ in
the tested cells. Closing this gap would require a matched-recall
tuning study; at the reported settings, their speedups should be
interpreted as lower-recall operating points rather than exact-graph
replacements. Second, GGNN's highest-quality setting drops sharply at
$d{=}5$ before recovering at higher dimensions; the text notes that
behavior explicitly rather than adding another table column.

\begin{figure}[!htbp]
\centering
\begin{minipage}[t]{0.49\textwidth}
  \centering
  \includegraphics[width=\linewidth]{recall_exactness_proof.png}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\textwidth}
  \centering
  \includegraphics[width=\linewidth]{recall_speed_pareto.png}
\end{minipage}
  \caption{Recall evaluation on HGCAL data. \emph{Left:}
  representative distance-based recall at $d{=}3$,
  $N{=}5\times10^5$, $k{=}40$; FastGraph and FAISS-GPU reach
  recall $=1.0$ under their kernel-matched exact distance
  conventions. \emph{Right:} recall-speed Pareto at the same
  operating point; approximate methods are only competitive when
  lower recall is acceptable.}
  \label{fig:recall_eval}
\end{figure}

\subsection{Dynamic graph construction in GNNs}

GravNet-style layers~\cite{Qasim2019GravNet} construct a fresh kNN
graph in a learned latent space at every forward pass, so the
kNN substrate is on the gradient path of every training step.
FastGraph integrates into this setting through \texttt{GravNetOp}, a
PyTorch module that wraps coordinate transformation, kNN graph
construction (via the opt-in \texttt{use\_pca=True} PCA-subspace path of
Section~\ref{sec:method}), and message passing into a single
autograd-compatible operation. Gradients flow through the selected
distances and the input coordinates; the discrete neighbor indices
and the per-call projection $V_k$ are held fixed across the
forward-backward pair so that the gradients are deterministic within
a step. Object condensation~\cite{Kieseler2020ObjectCondensation},
which is the loss used together with GravNet for HGCAL
reconstruction, is supported through a separate GPU-resident helper
kernel described in the appendix.

\section{Limitations}
\label{sec:limitations}

\paragraph*{Resolved axis-aligned limitations.}
The $d_{\max}{=}5$ binning cap inherited from axis-aligned cell-list
implementations is removed by the PCA-subspace formulation: the
projected grid is $n_{\text{bins}}^{k}$ in the binning subspace size
$k$, independent of the input dimensionality $d$. Per-column scale
heterogeneity is also absorbed into the variance ordering of the
principal components, so the binning structure no longer has to be
told which input axes are large.

\paragraph*{Data distribution still matters.}
The PCA contribution's effectiveness is a function of how much
pairwise-distance variance the leading components actually capture.
On strongly anisotropic data---HGCAL recHits, anything with a few
dominant axes---FastGraph's PCA-subspace binning is substantially
faster than axis-aligned cell lists (Section~\ref{sec:mbd-ablation},
geomean speedups in Section~\ref{sec:headline}). On isotropic data, the
leading subspace is an arbitrary orthonormal $k$-dimensional projection and FastGraph
degrades gracefully toward ordinary cell-list behavior; in that regime
FastGraph outperforms dense GPU brute force
only while the cell-list pruning advantage outweighs the constant
factor of an optimized BLAS \texttt{gemm}. Section~\ref{sec:synthetic}
documents this on Gaussian $\mathcal{N}(0, I_d)$ inputs and is the
reason we do not claim FastGraph is the fastest GPU exact kNN on
arbitrary distributions; we claim it is the fastest among the tested
exact GPU baselines on HGCAL-like workloads where the leading
principal components carry real structural information, which is the
regime modern learned-latent dynamic graphs are designed to live in.

\paragraph*{Remaining limitations.}
PCA estimation itself adds an $O(N d^2)$ term that is amortised over
the kNN query in the regimes we benchmark, but at very small $N$ the
projection cost becomes a non-negligible fraction of the total
wall-clock; for tiny per-event point counts an axis-aligned variant
remains preferable. The current implementation targets a single
GPU; multi-GPU or distributed kNN graph construction is not
supported. The discrete neighbor indices are not differentiable;
FastGraph's autograd contract is over the continuous distances and
coordinates only, matching every other hard-selection kNN layer.
Among the GPU baselines, approximate methods remain faster than
FastGraph at default low-recall settings; FastGraph is the right
choice only when exact graph construction is required.

\paragraph*{Why exactness matters for the downstream GNN.}
The case for an exact $k$NN backend over a recall-tuned approximate
one is not a raw-recall argument but a sensitivity-of-downstream
argument. Adversarial-robustness studies of graph neural networks
show that node predictions can shift substantially under small,
structure-preserving edge perturbations to the input
graph~\cite{zugner2018adversarial}; the same literature also
documents that systematic reweighting of the neighbourhood---e.g.,
replacing direct neighbours with a diffusion-smoothed neighbour
set---measurably alters downstream accuracy~\cite{klicpera2019diffusion}.
The neighbour-set mismatches injected by an approximate $k$NN are
exactly this kind of perturbation, distributed non-uniformly across
the event and dependent on the build-side index hyperparameters.
We do not claim the HGCAL reconstruction targets are unusually
sensitive to approximate-$k$NN edge noise---that would require a
dedicated model study beyond the scope of this paper---but the more
conservative choice for
a physics application that demands reproducible, systematics-controlled
inference is the exact graph, particularly when an exact backend is
competitive on wall-clock in the relevant operating regime
(Section~\ref{sec:headline}).

\paragraph*{CAGRA on heterogeneous data.}
CAGRA's default IVF-PQ build path fails on HGCAL data at $d \ge 5$
(Section~\ref{sec:expsetup}); the NN-Descent fallback succeeds
across the full dimensional sweep but is \spdHGCALcagradeight\
slower than FastGraph at the $d{=}8$ reference cell. We report the
NN-Descent numbers throughout. This is a finding about CAGRA on
heterogeneous-scale features, not a generic CAGRA limitation;
practitioners deploying CAGRA on similarly heterogeneous physics
datasets should be aware of the failure mode and the appropriate
build-path mitigation.

\section{Data and Code Availability Statement}
\begingroup
\sloppy
The FastGraph library, including the PCA-subspace extension described in
this paper, is released at
\url{https://github.com/AgarwalAarush/FastGraphCompute}, as a fork of
the upstream \texttt{FastGraphCompute} library.
The exact source used to collect every headline timing in this paper is
the \texttt{paper-release} branch built against the CUDA 12.1
toolchain. The released kernel surface covers binning subspace size
$k \in \{2, 3, 4, 5\}$. The benchmarking harness and runner scripts
used to produce the figures live in a companion repository,
\url{https://github.com/AgarwalAarush/fgc-performance}. The CLOVER
head-to-head used a fork of
\url{https://github.com/ampslab/clover-knn} at
\url{https://github.com/AgarwalAarush/clover-knn} with local source
modifications (\texttt{k\_values} aligned to the paper's $k{=}40$, the
hardcoded synthetic-N template chain in \texttt{main()} disabled in favor
of the mesh path, and the build retargeted to \texttt{sm\_80} for A100).
\endgroup

The detector-derived benchmarks use CMS HGCAL simulation data (recHit
feature vectors) accessed via an internal collaboration dataset of
\hgcalRecHits\ calorimeter hits with up to ten spatial and energy
features per hit. The data itself is internal to the CMS collaboration;
synthetic Gaussian and uniform datasets used in
Sections~\ref{sec:synthetic} and~\ref{sec:clover} are generated on the
fly from fixed random seeds and are fully reproducible from the released
code. CMS collaboration members can reproduce the HGCAL results
directly; external parties should contact the corresponding author.

\section{Conclusion}

We have introduced FastGraph, a PCA-subspace binned exact $k$-nearest-
neighbor graph construction primitive for GPU geometric deep learning.
The PCA-subspace formulation extends classical cell-list $k$NN past
the small-dimensional cap that previously limited axis-aligned
implementations, while a contraction argument
($V_k V_k^\top \preceq I_d$) preserves exactness in the original
input space. On the HGCAL recHit workloads that motivate this paper,
across $d{=}2$--$10$ at the headline $N{=}5$\,M, $k{=}40$ operating
point, FastGraph is the fastest among the tested \emph{exact} GPU
$k$NN baselines at every tested dimension, with speedups up to
$40\times$ over
FAISS-GPU \texttt{IndexFlatL2}, $20\times$ over cuVS brute force
(the strongest exact GPU baseline), and $17\times$ over the
approximate CAGRA-NN-Descent build---which is itself the only CAGRA
path that runs at $d \ge 5$ on heterogeneous-scale detector data. The ablation
in Section~\ref{sec:mbd-ablation} isolates the PCA rotation rather
than incidental implementation differences as the primary
contribution: comparing each variant at its own optimum gives a
within-paper ratio of \fgcVanillaBestPCAfactor\ between PCA-subspace
and axis-aligned binning at $d{=}8$, $N{=}5$\,M, $k{=}40$.

This paper does not claim that FastGraph is the fastest GPU $k$NN on
every distribution or every dimensionality. Two boundaries of the
claim are documented. First, the deployment regime targeted and measured
here is the $d{=}4$--$10$ learned latent space used by GravNet and HGCAL
multi-particle reconstruction; wider feature-space graph layers require
separate evaluation. The 3D-only spatial-acceleration methods (CLOVER,
LBVH, RT-cores) are unavailable in the $d \ge 4$ regime by
construction, and at the largest $d{=}3$ HGCAL head-to-head point
($N{=}5$\,M, $k{=}40$), the CLOVER spatio-graph index is about
$100\times$ faster than FastGraph (Section~\ref{sec:clover}). In
that case FastGraph is the ordinary 3D axis-aligned cell list, not
the PCA-subspace path. The $d{=}3$ detector-data case does not
overlap with the regime FastGraph is designed for, but it delineates
the boundary of the claim. Second, on \emph{isotropic} data the PCA
projection has no structure to exploit and FastGraph degrades toward
ordinary cell-list behavior; for that case the relative ordering
against dense BLAS brute force is determined by the cell-list
pruning advantage alone, not by the PCA contribution. The HGCAL
gains are an anisotropic-data result, and PCA-subspace binning
should be a generally useful substrate for dynamic graph layers in
scientific GNNs whose latent spaces have similar spectral structure.

\bibliographystyle{elsarticle-num}
\bibliography{references}

\appendix

\section{Object Condensation Helper --- Implementation Details}
\label{app:oc}

In addition to the kNN primitive, the library provides a GPU-resident
CUDA kernel for the object condensation
loss~\cite{Kieseler2020ObjectCondensation}, widely used to cluster
sensor-hit point clouds into particle candidates in physics
reconstruction. Its training loop requires two dense auxiliary
index matrices to be recomputed at every forward pass:

\begin{enumerate}
  \item The \emph{association matrix}
  $M \in \mathbb{Z}^{n_{\text{unique}} \times n_{\max uq}}$, whose
  $k$-th row lists the vertex indices assigned to the $k$-th object
  candidate.
  \item The optional \emph{complement matrix}
  $M_{\lnot} \in \mathbb{Z}^{n_{\text{unique}} \times n_{\max rs}}$,
  listing vertices \emph{not} assigned to the candidate; required
  only when the repulsive term $m_{\lnot}$ is included in the loss.
\end{enumerate}

Both matrices must be recomputed every forward pass because the
predicted associations and the ragged batch layout change from
iteration to iteration. The CUDA kernel parallelizes over unique
object candidates, assigning one thread per candidate, eliminating
atomic operations and thread synchronization and ensuring
collision-free writes to the output matrices at the cost of
$O(n_{\text{unique}})$ launched threads per forward pass.

\begin{algorithm}[H]
\DontPrintSemicolon
\KwIn{\texttt{asso\_idx}[$n_v$], \texttt{unique\_idx}[$n_u$], \texttt{unique\_rs\_asso}[$n_u$], \texttt{rs}[$n_s{+}1$], $n_v, n_u, n_{\max uq}, n_{\max rs}$, flag \texttt{calc\_m\_not}}
\KwOut{$M$[$n_u$][$n_{\max uq}$], $M_{\lnot}$[$n_u$][$n_{\max rs}$]}
$k \leftarrow \texttt{blockIdx.x} \cdot \texttt{blockDim.x} + \texttt{threadIdx.x}$\;
\If{$k \geq n_u$}{\Return}
$uq \leftarrow \texttt{unique\_idx}[k]$;\quad
$split \leftarrow \texttt{unique\_rs\_asso}[k]$;\quad
$start \leftarrow \texttt{rs}[split]$;\quad
$end \leftarrow \min(\texttt{rs}[split{+}1],\; n_v)$\;
\If{$end - start > n_{\max rs}$}{$end \leftarrow start + n_{\max rs}$}
$fill \leftarrow 0$\;
\For{$i \leftarrow start$ \KwTo $end-1$}{
  \If{$\texttt{asso\_idx}[i] = uq$}{
    $M[k][fill] \leftarrow i$;\quad $fill \leftarrow fill + 1$\;
    \If{$fill \geq n_{\max uq}$}{\Break}
  }
}
\While{$fill < n_{\max uq}$}{$M[k][fill] \leftarrow -1$;\quad $fill \leftarrow fill + 1$}
\If{\texttt{calc\_m\_not}}{
  $fill \leftarrow 0$\;
  \For{$i \leftarrow start$ \KwTo $end-1$}{
    \If{$\texttt{asso\_idx}[i] \neq uq$}{
      $M_{\lnot}[k][fill] \leftarrow i$;\quad $fill \leftarrow fill + 1$\;
      \If{$fill \geq n_{\max rs}$}{\Break}
    }
  }
  \While{$fill < n_{\max rs}$}{$M_{\lnot}[k][fill] \leftarrow -1$;\quad $fill \leftarrow fill + 1$}
}
\caption{CUDA Object Condensation Helper (\texttt{oc\_helper}): one thread per candidate.}
\label{alg:oc}
\end{algorithm}

\end{document}
